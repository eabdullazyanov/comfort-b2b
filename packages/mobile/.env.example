# Optional for local development — in dev builds the app defaults to the
# platform-correct localhost automatically (10.0.2.2 on Android emulator,
# localhost on iOS simulator).
#
# Set this to point at a non-localhost backend (real device, staging, etc.):
#
# Required in production: a missing value throws at startup.
